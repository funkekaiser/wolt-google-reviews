{
  "name": "wolt-google-reviews",
  "private": true,
  "workspaces": [
    "extension"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspace extension",
    "package": "npm run package --workspace extension"
  }
}
