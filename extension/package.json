{
  "name": "wolt-google-reviews-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "API_BASE=https://wolt-google-reviews.funkekaiser.workers.dev node build.mjs --release && cd dist && rm -f ../rating-lens-for-wolt.zip && zip -qr ../rating-lens-for-wolt.zip .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
