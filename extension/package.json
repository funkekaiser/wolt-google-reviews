{
  "name": "wolt-google-reviews-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "node build.mjs --release && cd dist && rm -f ../rating-lens-for-wolt.zip && zip -qr ../rating-lens-for-wolt.zip .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
